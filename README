# GitHub Fundamental Task

## Things I have learnt 💖

+ How to use Command Lines
+ Configuring SSH Key
+ Learnt more about Git and GitHub - such as the git commit short cut command:

```
git commit -am "commit message"
```
+ git branching for git best practice to experiment and fix bugs:

```
git branch <new branch name>` - to create a new branch
git checkout to switch branches
```
> Alternatively use:

```
git checkout -b <new branch name> - to create and switch to another branch with all the history of the branch currently working on
```

## Things I am Still Confused About 🤔
+ git pull and merging request